      <main class="flex-1 flex flex-col min-w-0 overflow-hidden bg-slate-950">

        <!-- Toolbar -->
        <div x-show="currentBox" class="h-14 border-b border-slate-800 bg-slate-900/40 px-4 flex items-center justify-between flex-shrink-0 gap-3">
          <div class="flex items-center gap-3 min-w-0">
            <h1 class="font-bold text-base text-slate-100 truncate" x-text="currentBox?.name || ''"></h1>
            <span x-show="currentBox" class="text-[10px] px-2 py-0.5 rounded-full border uppercase font-semibold flex-shrink-0"
                  :class="currentBox?.visibility === 'public' ? 'bg-emerald-500/10 text-emerald-400 border-emerald-500/30' : 'bg-slate-800 text-slate-400 border-slate-700'"
                  x-text="currentBox?.visibility"></span>
            <span x-show="currentBox?.turso_status === 'ready'"
                  class="text-[10px] px-2 py-0.5 rounded-full bg-emerald-500/10 text-emerald-400 border border-emerald-500/30 flex items-center gap-1 flex-shrink-0">
              <span class="w-1 h-1 rounded-full bg-emerald-400"></span> DB lista
            </span>
          </div>

          <div x-show="currentBox" class="flex items-center gap-1 bg-slate-900 p-1 rounded-xl border border-slate-800 flex-shrink-0">
            <button @click="switchTab('preview')"
                    :class="activeTab === 'preview' ? 'bg-indigo-600 text-white shadow' : 'text-slate-400 hover:text-white'"
                    class="px-3 py-1.5 rounded-lg text-xs font-medium transition flex items-center gap-2">
              <i class="fa-solid fa-play text-xs"></i><span class="hidden sm:inline">Vista Previa</span>
            </button>
            <button @click="switchTab('editor')"
                    :class="activeTab === 'editor' ? 'bg-indigo-600 text-white shadow' : 'text-slate-400 hover:text-white'"
                    class="px-3 py-1.5 rounded-lg text-xs font-medium transition flex items-center gap-2">
              <i class="fa-solid fa-code text-xs"></i><span class="hidden sm:inline">Editor HTML</span>
            </button>
            <button @click="switchTab('data')"
                    :class="activeTab === 'data' ? 'bg-indigo-600 text-white shadow' : 'text-slate-400 hover:text-white'"
                    class="px-3 py-1.5 rounded-lg text-xs font-medium transition flex items-center gap-2">
              <i class="fa-solid fa-database text-xs"></i><span class="hidden sm:inline">Archivos &amp; Datos</span>
            </button>
            <button @click="switchTab('users')"
                    :class="activeTab === 'users' ? 'bg-indigo-600 text-white shadow' : 'text-slate-400 hover:text-white'"
                    class="px-3 py-1.5 rounded-lg text-xs font-medium transition flex items-center gap-2">
              <i class="fa-solid fa-users text-xs"></i><span class="hidden md:inline">Usuarios</span>
            </button>
            <button @click="switchTab('mcp')"
                    :class="activeTab === 'mcp' ? 'bg-indigo-600 text-white shadow' : 'text-slate-400 hover:text-white'"
                    class="px-3 py-1.5 rounded-lg text-xs font-medium transition flex items-center gap-2">
              <i class="fa-solid fa-robot text-xs text-purple-400"></i><span class="hidden md:inline">Agentes (MCP)</span>
            </button>
          </div>

          <div x-show="currentBox" class="flex items-center gap-2 flex-shrink-0">
            <button @click="openShareModal()" class="px-3 py-1.5 rounded-xl text-slate-300 hover:text-white hover:bg-slate-800 border border-slate-800 text-xs font-medium flex items-center gap-1.5 transition">
              <i class="fa-solid fa-up-right-from-square text-indigo-400"></i>
              <span class="hidden sm:inline">Probar Enlace</span>
            </button>
            <button @click="openBoxWithDebug()" class="p-1.5 rounded-xl text-slate-400 hover:text-white hover:bg-slate-800 border border-slate-800 transition" title="Abrir box con panel de debug (?hbx_debug=1)">
              <i class="fa-solid fa-bug"></i>
            </button>
            <button @click="copyShareLink()" class="p-1.5 rounded-xl text-slate-400 hover:text-white hover:bg-slate-800 border border-slate-800 transition" title="Copiar enlace público">
              <i class="fa-solid fa-share-nodes"></i>
            </button>
            <button @click="saveCurrentBox()" :disabled="saving"
                    class="px-3 py-1.5 bg-emerald-600 hover:bg-emerald-500 disabled:bg-slate-700 text-white font-medium text-xs rounded-xl shadow-lg shadow-emerald-600/20 flex items-center gap-1.5 transition">
              <template x-if="saving"><i class="fa-solid fa-spinner animate-spin"></i></template>
              <template x-if="!saving"><i class="fa-solid fa-floppy-disk"></i></template>
              <span x-text="saving ? 'Guardando…' : 'Guardar'"></span>
            </button>
          </div>
        </div>

        <!-- Tab content -->
        <div class="flex-1 overflow-hidden relative">

          <!-- Preview -->
          <div x-show="activeTab === 'preview'" class="absolute inset-0 flex flex-col bg-slate-900">
            <iframe x-show="currentBox" :srcdoc="previewSrcdoc" sandbox="allow-scripts allow-modals allow-forms" class="w-full h-full border-0 bg-white"></iframe>
            <div x-show="!currentBox" class="flex items-center justify-center h-full text-slate-500 text-sm">
              Seleccioná o creá un box para previsualizarlo.
            </div>
          </div>

          <!-- Editor -->
          <div x-show="activeTab === 'editor'" class="absolute inset-0 flex flex-col bg-slate-950">
            <div class="bg-slate-900 border-b border-slate-800 px-4 py-2 flex justify-between items-center text-xs text-slate-400 font-mono">
              <div class="flex items-center gap-3">
                <span class="flex items-center gap-1.5">
                  <i class="fa-brands fa-html5 text-orange-400"></i> index.html
                </span>
                <span x-show="currentBox" class="text-slate-500">
                  versión <span class="text-indigo-400" x-text="currentBox?.htmlbox_version ?? 0"></span>
                </span>
                <span x-show="aiAutoAnalyzePending" class="flex items-center gap-1.5 text-purple-400">
                  <i class="fa-solid fa-wand-magic-sparkles fa-beat"></i>
                  <span>IA trabajando en segundo plano…</span>
                </span>
              </div>
              <div class="flex gap-3 items-center">
                <button @click="formatHTML()" class="hover:text-white transition" title="Re-formatear indentación">
                  <i class="fa-solid fa-wand-magic-sparkles mr-1"></i> Formatear
                </button>
                <button @click="aiAnalyze()" :disabled="aiAnalyzing || !editorHtml.trim() || !currentBox"
                        class="px-3 py-1 bg-purple-600 hover:bg-purple-500 disabled:bg-slate-700 rounded-lg text-xs font-medium transition flex items-center gap-1">
                  <i class="fa-solid" :class="aiAnalyzing ? 'fa-spinner fa-spin' : 'fa-wand-magic-sparkles'"></i>
                  <span x-text="aiAnalyzing ? 'Analizando…' : 'Analizar con IA'"></span>
                </button>
                <span class="text-slate-700">|</span>
                <span><span x-text="editorHtml.length"></span> caracteres</span>
                <label class="hidden lg:flex items-center gap-2 text-xs text-slate-400 ml-1 pl-3 border-l border-slate-800">
                  <input type="checkbox" :checked="!!(currentBox && currentBox.auto_analyze_on_save)" @change="toggleAutoAnalyze($event)"
                         :disabled="updatingAutoAnalyze || !currentBox" class="rounded">
                  Auto-IA al guardar
                </label>
              </div>
            </div>
            <div x-ref="monacoContainer"
                 x-effect="activeTab === 'editor' && mountMonacoIfNeeded()"
                 class="flex-1 w-full code-editor"></div>
          </div>

          <!-- Tabla & Ingesta de Datos (fase 3 v1) -->
          <div x-show="activeTab === 'data'" class="absolute inset-0 p-6 overflow-y-auto bg-slate-950">
            <div class="max-w-4xl mx-auto space-y-4" x-data="dataTab()" x-ref="dataTabRef" x-init="init()">

              <!-- Header con acciones -->
              <div class="flex items-center justify-between">
                <div>
                  <h2 class="text-lg font-bold flex items-center gap-2">
                    <i class="fa-solid fa-database text-indigo-400"></i>
                    Tablas &amp; Datos
                  </h2>
                  <p class="text-xs text-slate-400 mt-1">
                    Subí CSV/XLSX/JSON. El HTML del box los lee con
                    <code class="text-indigo-300">HTMLBox.table('&lt;slug&gt;').rows()</code>.
                  </p>
                </div>
                <button @click="showNewTable = true"
                        class="px-3 py-1.5 bg-indigo-600 hover:bg-indigo-500 rounded-lg text-xs font-medium transition">
                  <i class="fa-solid fa-plus mr-1"></i> Nueva tabla
                </button>
              </div>

              <!-- Form nueva tabla -->
              <div x-show="showNewTable" x-transition
                   class="p-4 rounded-2xl bg-slate-900 border border-slate-800 space-y-3">
                <h3 class="text-xs font-bold text-slate-400 uppercase tracking-wider">Crear tabla</h3>
                <div class="flex gap-2">
                  <input x-model="newTableSlug" placeholder="slug (ej: ventas, productos, clientes)"
                           class="flex-1 bg-slate-950 border border-slate-800 rounded-lg px-3 py-1.5 text-sm focus:outline-none focus:border-indigo-500">
                  <input x-model="newTableName" placeholder="Nombre (opcional)"
                           class="flex-1 bg-slate-950 border border-slate-800 rounded-lg px-3 py-1.5 text-sm focus:outline-none focus:border-indigo-500">
                  <select x-model="newTableScope"
                          class="bg-slate-950 border border-slate-800 rounded-lg px-3 py-1.5 text-sm focus:outline-none focus:border-indigo-500">
                    <option value="private">Private — cada customer ve solo lo suyo</option>
                    <option value="shared">Shared — todos ven lo mismo (catálogo)</option>
                  </select>
                  <button @click="createTable()" :disabled="!newTableSlug || creatingTable"
                          class="px-4 py-1.5 bg-indigo-600 hover:bg-indigo-500 disabled:bg-slate-700 rounded-lg text-sm font-medium transition">
                    <span x-show="!creatingTable">Crear</span>
                    <span x-show="creatingTable"><i class="fa-solid fa-spinner fa-spin"></i></span>
                  </button>
                  <button @click="showNewTable = false"
                          class="px-3 py-1.5 bg-slate-800 hover:bg-slate-700 rounded-lg text-sm transition">Cancelar</button>
                </div>
                <p class="text-[10px] text-slate-500">El slug debe ser minúsculas, sin espacios. "Private" filtra por usuario (default); "Shared" todos ven lo mismo.</p>
              </div>

              <!-- Dropzone upload -->
              <div x-show="activeTable"
                   class="p-4 rounded-2xl bg-slate-900 border border-slate-800 space-y-3">
                <div class="flex items-center justify-between">
                  <h3 class="text-xs font-bold text-slate-400 uppercase tracking-wider">
                    Subir archivo a <code class="text-indigo-300" x-text="activeTable"></code>
                  </h3>
                  <button @click="activeTable = null"
                          class="text-slate-500 hover:text-white text-xs"><i class="fa-solid fa-times"></i></button>
                </div>
                <div @dragover.prevent @drop.prevent="handleDrop($event)"
                     @click="$refs.fileInput.click()"
                     class="border-2 border-dashed border-slate-700 hover:border-indigo-500 rounded-xl p-6 text-center cursor-pointer transition">
                  <i class="fa-solid fa-cloud-upload-arrow text-2xl text-slate-500 mb-2"></i>
                  <p class="text-sm text-slate-400">
                    Arrastrá un archivo CSV, XLSX o JSON, o hacé click para elegir
                  </p>
                  <p class="text-[10px] text-slate-500 mt-1">
                    Estrategia:
                    <select x-model="uploadStrategy" class="bg-slate-950 border border-slate-800 rounded px-1 text-[10px]">
                      <option value="replace">replace (borrar todo y recargar)</option>
                      <option value="upsert">upsert (agregar)</option>
                    </select>
                  </p>
                  <input type="file" x-ref="fileInput" class="hidden"
                         accept=".csv,.json,.txt" @change="handleFile($event)">
                </div>
                <div x-show="uploadStatus" x-text="uploadStatus"
                     class="text-xs px-3 py-2 rounded-lg"
                     :class="uploadOk ? 'bg-emerald-900/30 text-emerald-300' : 'bg-rose-900/30 text-rose-300'"></div>
              </div>

              <!-- Lista de tablas -->
              <div class="space-y-2">
                <template x-for="t in tables" :key="t.slug">
                  <div class="p-4 rounded-2xl bg-slate-900/60 border border-slate-800">
                    <div class="flex items-center justify-between">
                      <div>
                        <h3 class="font-medium text-sm">
                          <i class="fa-solid fa-table text-indigo-400 mr-2"></i>
                          <span x-text="t.name || t.slug"></span>
                          <span class="text-[9px] px-1.5 py-0.5 rounded ml-2 font-semibold"
                                :class="(t.scope || 'private') === 'private' ? 'bg-amber-500/10 text-amber-400 border border-amber-500/30' : 'bg-sky-500/10 text-sky-400 border border-sky-500/30'"
                                x-text="(t.scope || 'private') === 'private' ? 'PRIVATE' : 'SHARED'"></span>
                        </h3>
                        <p class="text-[10px] text-slate-500 mt-0.5">
                          slug: <code class="text-slate-400" x-text="t.slug"></code>
                          · <span x-text="t.mode"></span>
                          · <span x-text="t.created_at"></span>
                        </p>
                      </div>
                      <div class="flex gap-1">
                        <button @click="toggleTableScope(t)"
                                class="px-3 py-1 bg-slate-800 hover:bg-slate-700 rounded-lg text-[11px] transition"
                                :title="'Cambiar a ' + ((t.scope || 'private') === 'private' ? 'shared' : 'private')">
                          <i class="fa-solid fa-arrows-rotate mr-1"></i>
                          <span x-text="(t.scope || 'private') === 'private' ? 'private' : 'shared'"></span>
                        </button>
                        <button @click="selectTable(t)"
                                class="px-3 py-1 bg-slate-800 hover:bg-indigo-600 rounded-lg text-[11px] transition">
                          Subir archivo
                        </button>
                        <button @click="previewTable(t)"
                                class="px-3 py-1 bg-slate-800 hover:bg-slate-700 rounded-lg text-[11px] transition">
                          Ver filas
                        </button>
                      </div>
                    </div>
                    <div x-show="previewSlug === t.slug" class="mt-3" x-transition>
                      <div x-show="previewLoading" class="text-xs text-slate-500 py-2">
                        <i class="fa-solid fa-spinner fa-spin mr-1"></i> Cargando…
                      </div>
                      <div x-show="!previewLoading && previewRows.length === 0" class="text-xs text-slate-500 italic py-2">
                        Tabla vacía. Subí un archivo para verla acá.
                      </div>
                      <div x-show="!previewLoading && previewRows.length > 0" class="overflow-x-auto">
                        <table class="w-full text-left text-[11px]">
                          <thead class="text-slate-500 border-b border-slate-800">
                            <tr>
                              <template x-for="c in previewColumns" :key="c">
                                <th class="p-2 font-medium" x-text="c"></th>
                              </template>
                            </tr>
                          </thead>
                          <tbody class="divide-y divide-slate-800/60">
                            <template x-for="r in previewRows" :key="r.id">
                              <tr>
                                <template x-for="c in previewColumns" :key="c">
                                  <td class="p-2 text-slate-300" x-text="r[c] !== undefined ? String(r[c]).slice(0, 80) : ''"></td>
                                </template>
                              </tr>
                            </template>
                          </tbody>
                        </table>
                      </div>
                    </div>
                  </div>
                </template>
                <div x-show="tables.length === 0 && !loadingTables" class="text-xs text-slate-500 italic text-center py-6">
                  No hay tablas todavía. Creá una con "Nueva tabla" arriba.
                </div>
                <div x-show="loadingTables" class="text-xs text-slate-500 text-center py-6">
                  <i class="fa-solid fa-spinner fa-spin mr-1"></i> Cargando…
                </div>
              </div>

            </div>
          </div>

          <!-- Tab Usuarios (fase 1 — htmlbox-spec-app-users.md / app-customers.md) -->
          <div x-show="activeTab === 'users'" class="absolute inset-0 p-6 overflow-y-auto bg-slate-950">
            <div class="max-w-4xl mx-auto space-y-4" x-data="usersTab()" x-ref="usersTabRef" x-init="init()">

              <div class="flex items-center justify-between">
                <div>
                  <h2 class="text-lg font-bold flex items-center gap-2">
                    <i class="fa-solid fa-users text-indigo-400"></i>
                    Usuarios de la app
                  </h2>
                  <p class="text-xs text-slate-400 mt-1">
                    Gente que puede entrar al box publicado con su propio email (vía magic link).
                    Cada alta vive en la DB aislada del box.
                  </p>
                </div>
                <button @click="showAddForm = !showAddForm"
                        class="px-3 py-1.5 bg-indigo-600 hover:bg-indigo-500 rounded-lg text-xs font-medium transition">
                  <i class="fa-solid fa-plus mr-1"></i> Agregar usuario
                </button>
              </div>

              <!-- Toggle signup_mode (fase 2 — htmlbox-spec-app-customers.md §3) -->
              <div class="p-4 rounded-2xl bg-slate-900/60 border border-slate-800 flex items-center justify-between">
                <div>
                  <h3 class="font-medium text-sm">
                    <i class="fa-solid fa-user-plus text-indigo-400 mr-2"></i>
                    Auto-registro
                  </h3>
                  <p class="text-[10px] text-slate-500 mt-0.5">
                    Si está activo, cualquiera puede pedir un magic link y se crea la cuenta al toque
                    (modo ecommerce / customer-facing). Si está apagado, solo pueden entrar los emails
                    que vos agregues a mano acá.
                  </p>
                </div>
                <label class="relative inline-flex items-center cursor-pointer">
                  <input type="checkbox" :checked="signupMode === 'open'" @change="toggleSignupMode()"
                         :disabled="updatingSignupMode" class="sr-only peer">
                  <div class="w-11 h-6 bg-slate-700 peer-focus:outline-none rounded-full peer
                              peer-checked:after:translate-x-full peer-checked:after:border-white
                              after:content-[''] after:absolute after:top-[2px] after:left-[2px]
                              after:bg-white after:rounded-full after:h-5 after:w-5 after:transition-all
                              peer-checked:bg-indigo-600"></div>
                  <span class="ml-3 text-xs font-medium text-slate-300"
                        x-text="signupMode === 'open' ? 'Abierto' : 'Solo invitados'"></span>
                </label>
              </div>

              <!-- Form alta -->
              <div x-show="showAddForm" x-transition
                   class="p-4 rounded-2xl bg-slate-900 border border-slate-800 space-y-3">
                <h3 class="text-xs font-bold text-slate-400 uppercase tracking-wider">Agregar app-user</h3>
                <div class="grid grid-cols-1 sm:grid-cols-2 gap-2">
                  <input x-model="newEmail" placeholder="email@ejemplo.com"
                         class="bg-slate-950 border border-slate-800 rounded-lg px-3 py-1.5 text-sm focus:outline-none focus:border-indigo-500">
                  <input x-model="newDisplayName" placeholder="Nombre (opcional)"
                         class="bg-slate-950 border border-slate-800 rounded-lg px-3 py-1.5 text-sm focus:outline-none focus:border-indigo-500">
                </div>
                <div class="flex gap-2">
                  <button @click="addUser()" :disabled="!newEmail.trim() || addingUser"
                          class="px-4 py-1.5 bg-indigo-600 hover:bg-indigo-500 disabled:bg-slate-700 rounded-lg text-sm font-medium transition">
                    <span x-show="!addingUser">Agregar</span>
                    <span x-show="addingUser"><i class="fa-solid fa-spinner fa-spin"></i></span>
                  </button>
                  <button @click="showAddForm = false"
                          class="px-3 py-1.5 bg-slate-800 hover:bg-slate-700 rounded-lg text-sm transition">Cancelar</button>
                </div>
                <p class="text-[10px] text-slate-500">
                  Una vez agregado, esa persona puede pedir un magic link desde el box publicado.
                  Si el modo es "Abierto" este paso no hace falta (se auto-crean al pedir el link).
                </p>
              </div>

              <!-- Lista -->
              <div class="space-y-2">
                <template x-for="u in users" :key="u.id">
                  <div class="p-4 rounded-2xl bg-slate-900/60 border border-slate-800 flex items-center justify-between">
                    <div class="min-w-0">
                      <h3 class="font-medium text-sm truncate" x-text="u.display_name || u.email"></h3>
                      <p class="text-[10px] text-slate-500 mt-0.5">
                        <code class="text-slate-400" x-text="u.email"></code>
                        · <span x-text="u.role"></span>
                        · <span x-text="u.disabled_at ? 'Deshabilitado' : 'Activo'"></span>
                      </p>
                    </div>
                    <div class="flex gap-1 flex-shrink-0">
                      <button x-show="!u.disabled_at" @click="setUserDisabled(u.id, true)"
                              class="px-3 py-1 bg-slate-800 hover:bg-amber-600 rounded-lg text-[11px] transition">
                        Deshabilitar
                      </button>
                      <button x-show="u.disabled_at" @click="setUserDisabled(u.id, false)"
                              class="px-3 py-1 bg-slate-800 hover:bg-emerald-600 rounded-lg text-[11px] transition">
                        Reactivar
                      </button>
                      <button @click="deleteUser(u)"
                              class="px-3 py-1 bg-slate-800 hover:bg-rose-600 rounded-lg text-[11px] transition">
                        <i class="fa-solid fa-trash"></i>
                      </button>
                    </div>
                  </div>
                </template>
                <div x-show="users.length === 0 && !loadingUsers" class="text-xs text-slate-500 italic text-center py-6">
                  No hay app-users todavía.
                </div>
                <div x-show="loadingUsers" class="text-xs text-slate-500 text-center py-6">
                  <i class="fa-solid fa-spinner fa-spin mr-1"></i> Cargando…
                </div>
              </div>

              <!-- Status -->
              <div x-show="usersStatus" x-text="usersStatus"
                   class="text-xs px-3 py-2 rounded-lg"
                   :class="usersStatusOk ? 'bg-emerald-900/30 text-emerald-300' : 'bg-rose-900/30 text-rose-300'"></div>

            </div>
          </div>

<!-- Tab Team (fase 3 — usuarios centralizados, tenant-level) -->
          <div x-show="activeTab === 'team'" class="absolute inset-0 p-6 overflow-y-auto bg-slate-950">
            <div class="max-w-4xl mx-auto space-y-4" x-data="teamTab()" x-ref="teamTabRef" x-init="init()">

              <div class="flex items-center justify-between">
                <div>
                  <h2 class="text-lg font-bold flex items-center gap-2">
                    <i class="fa-solid fa-user-tie text-indigo-400"></i>
                    Equipo Centralizado
                  </h2>
                  <p class="text-xs text-slate-400 mt-1">
                    Gente con acceso a múltiples boxes del tenant con un solo login (vía magic link).
                    Distinto de los "Usuarios" de cada box — esta lista es a nivel tenant y vive en
                    D1, no en la Turso de cada box.
                  </p>
                </div>
                <button @click="showAddForm = !showAddForm"
                        class="px-3 py-1.5 bg-indigo-600 hover:bg-indigo-500 rounded-lg text-xs font-medium transition">
                  <i class="fa-solid fa-plus mr-1"></i> Agregar persona
                </button>
              </div>

              <!-- Form alta -->
              <div x-show="showAddForm" x-transition
                   class="p-4 rounded-2xl bg-slate-900 border border-slate-800 space-y-3">
                <h3 class="text-xs font-bold text-slate-400 uppercase tracking-wider">Agregar al equipo</h3>
                <div class="grid grid-cols-1 sm:grid-cols-2 gap-2">
                  <input x-model="newEmail" placeholder="email@ejemplo.com"
                         class="bg-slate-950 border border-slate-800 rounded-lg px-3 py-1.5 text-sm focus:outline-none focus:border-indigo-500">
                  <input x-model="newDisplayName" placeholder="Nombre (opcional)"
                         class="bg-slate-950 border border-slate-800 rounded-lg px-3 py-1.5 text-sm focus:outline-none focus:border-indigo-500">
                </div>
                <div class="flex gap-2">
                  <button @click="addUser()" :disabled="!newEmail.trim() || addingUser"
                          class="px-4 py-1.5 bg-indigo-600 hover:bg-indigo-500 disabled:bg-slate-700 rounded-lg text-sm font-medium transition">
                    <span x-show="!addingUser">Agregar</span>
                    <span x-show="addingUser"><i class="fa-solid fa-spinner fa-spin"></i></span>
                  </button>
                  <button @click="showAddForm = false"
                          class="px-3 py-1.5 bg-slate-800 hover:bg-slate-700 rounded-lg text-sm transition">Cancelar</button>
                </div>
              </div>

              <!-- Lista -->
              <div class="space-y-2">
                <template x-for="u in users" :key="u.id">
                  <div class="p-4 rounded-2xl bg-slate-900/60 border border-slate-800">
                    <div class="flex items-center justify-between">
                      <div class="min-w-0">
                        <h3 class="font-medium text-sm truncate" x-text="u.display_name || u.email"></h3>
                        <p class="text-[10px] text-slate-500 mt-0.5">
                          <code class="text-slate-400" x-text="u.email"></code>
                          · <span x-text="u.disabled_at ? 'Deshabilitado' : 'Activo'"></span>
                        </p>
                      </div>
                      <div class="flex gap-1 flex-shrink-0">
                        <button @click="openGrant(u)"
                                class="px-3 py-1 bg-slate-800 hover:bg-indigo-600 rounded-lg text-[11px] transition">
                          <i class="fa-solid fa-key mr-1"></i> Otorgar acceso
                        </button>
                        <button @click="deleteUser(u)"
                                class="px-3 py-1 bg-slate-800 hover:bg-rose-600 rounded-lg text-[11px] transition">
                          <i class="fa-solid fa-trash"></i>
                        </button>
                      </div>
                    </div>

                    <!-- Lista de accesos del user -->
                    <div class="mt-3 space-y-1">
                      <template x-for="a in u.accesses" :key="a.id">
                        <div class="flex items-center justify-between p-2 rounded-lg bg-slate-950/50 border border-slate-800/60">
                          <div class="flex items-center gap-2 min-w-0">
                            <span class="text-[10px] px-1.5 py-0.5 rounded font-semibold flex-shrink-0"
                                  :class="a.scope_type === 'tenant' ? 'bg-purple-500/10 text-purple-400 border border-purple-500/30'
                                        : a.scope_type === 'workspace' ? 'bg-sky-500/10 text-sky-400 border border-sky-500/30'
                                        : 'bg-emerald-500/10 text-emerald-400 border border-emerald-500/30'"
                                  x-text="a.scope_type"></span>
                            <span class="text-[11px] text-slate-300 truncate" x-text="scopeLabel(a)"></span>
                            <span class="text-[10px] text-slate-500 font-mono" x-text="a.role"></span>
                          </div>
                          <button @click="revokeAccess(u, a)"
                                  class="px-2 py-0.5 bg-slate-800 hover:bg-rose-700 rounded text-[10px] transition flex-shrink-0">
                            <i class="fa-solid fa-times"></i>
                          </button>
                        </div>
                      </template>
                      <div x-show="u.accesses.length === 0" class="text-[10px] text-slate-500 italic">
                        Sin accesos otorgados.
                      </div>
                    </div>
                  </div>
                </template>
                <div x-show="users.length === 0 && !loadingTeam" class="text-xs text-slate-500 italic text-center py-6">
                  No hay gente en el equipo todavía.
                </div>
                <div x-show="loadingTeam" class="text-xs text-slate-500 text-center py-6">
                  <i class="fa-solid fa-spinner fa-spin mr-1"></i> Cargando…
                </div>
              </div>

              <!-- Modal grant -->
              <div x-show="grantModalOpen" x-transition.opacity
                   class="fixed inset-0 z-50 bg-black/60 flex items-center justify-center p-4"
                   @click.self="grantModalOpen = false">
                <div class="w-full max-w-md bg-slate-900 rounded-2xl border border-slate-800 p-6 space-y-4">
                  <h3 class="font-bold text-lg flex items-center gap-2">
                    <i class="fa-solid fa-key text-indigo-400"></i>
                    Otorgar acceso
                  </h3>
                  <p class="text-xs text-slate-400">
                    Para <strong x-text="grantTarget?.email"></strong>
                  </p>
                  <div class="space-y-2">
                    <label class="text-[10px] uppercase font-bold text-slate-400 block">Tipo de acceso</label>
                    <select x-model="grantForm.scope_type"
                            class="w-full bg-slate-950 border border-slate-800 rounded-lg px-3 py-2 text-sm">
                      <option value="box">Box puntual</option>
                      <option value="workspace">Workspace entero</option>
                      <option value="tenant">Tenant entero (todo)</option>
                    </select>
                  </div>
                  <div x-show="grantForm.scope_type !== 'tenant'" class="space-y-2">
                    <label class="text-[10px] uppercase font-bold text-slate-400 block">
                      <span x-show="grantForm.scope_type === 'box'">Box</span>
                      <span x-show="grantForm.scope_type === 'workspace'">Workspace</span>
                    </label>
                    <select x-model="grantForm.scope_id"
                            class="w-full bg-slate-950 border border-slate-800 rounded-lg px-3 py-2 text-sm">
                      <template x-if="grantForm.scope_type === 'box'">
                        <template x-for="b in boxes" :key="b.id">
                          <option :value="b.id" x-text="b.name + ' (' + b.slug + ')'"></option>
                        </template>
                      </template>
                      <template x-if="grantForm.scope_type === 'workspace'">
                        <template x-for="w in workspaces" :key="w.id">
                          <option :value="w.id" x-text="w.name"></option>
                        </template>
                      </template>
                    </select>
                  </div>
                  <div class="flex gap-2 justify-end">
                    <button @click="grantModalOpen = false"
                            class="px-3 py-1.5 bg-slate-800 hover:bg-slate-700 rounded-lg text-sm transition">Cancelar</button>
                    <button @click="grantAccess()" :disabled="granting"
                            class="px-4 py-1.5 bg-indigo-600 hover:bg-indigo-500 disabled:bg-slate-700 rounded-lg text-sm font-medium transition">
                      <span x-show="!granting">Otorgar</span>
                      <span x-show="granting"><i class="fa-solid fa-spinner fa-spin"></i></span>
                    </button>
                  </div>
                </div>
              </div>

              <!-- Status -->
              <div x-show="teamStatus" x-text="teamStatus"
                   class="text-xs px-3 py-2 rounded-lg"
                   :class="teamStatusOk ? 'bg-emerald-900/30 text-emerald-300' : 'bg-rose-900/30 text-rose-300'"></div>

            </div>
          </div>

<!-- MCP placeholder (fase 6) -->
          <div x-show="activeTab === 'mcp'" class="absolute inset-0 p-6 overflow-y-auto bg-slate-950">
            <div class="max-w-4xl mx-auto space-y-4">
              <div class="flex items-center justify-between">
                <div>
                  <h2 class="text-lg font-bold flex items-center gap-2">
                    <i class="fa-solid fa-microchip text-purple-400"></i>
                    Integración MCP
                  </h2>
                  <p class="text-xs text-slate-400">Exponé este HTML Box como herramienta estándar para ChatGPT, Claude, Gemini o tu agente corporativo.</p>
                </div>
                <button class="px-3 py-2 bg-slate-800 hover:bg-slate-700 text-slate-200 text-xs font-medium rounded-xl border border-slate-700 flex items-center gap-2 transition">
                  <i class="fa-solid fa-copy"></i>
                  <span>Copiar JSON Servidor MCP</span>
                </button>
              </div>
              <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
                <div class="p-4 rounded-2xl bg-slate-900 border border-slate-800 space-y-2">
                  <div class="flex justify-between items-center">
                    <span class="font-mono text-xs text-purple-400 font-bold">query_kpi_metrics()</span>
                    <span class="text-[10px] px-2 py-0.5 rounded bg-emerald-500/10 text-emerald-400 border border-emerald-500/20">Lectura</span>
                  </div>
                  <p class="text-xs text-slate-300">Obtiene los KPIs actualizados expuestos en el HTML del box.</p>
                  <div class="text-[11px] font-mono bg-slate-950 p-2 rounded text-slate-400">GET /api/v1/mcp/app-1/kpi</div>
                </div>
                <div class="p-4 rounded-2xl bg-slate-900 border border-slate-800 space-y-2">
                  <div class="flex justify-between items-center">
                    <span class="font-mono text-xs text-purple-400 font-bold">generate_spot_check()</span>
                    <span class="text-[10px] px-2 py-0.5 rounded bg-indigo-500/10 text-indigo-400 border border-indigo-500/20">Ejecución</span>
                  </div>
                  <p class="text-xs text-slate-300">Ejecuta análisis automático y devuelve reporte consolidado.</p>
                  <div class="text-[11px] font-mono bg-slate-950 p-2 rounded text-slate-400">POST /api/v1/mcp/app-1/spotcheck</div>
                </div>
              </div>
              <div class="bg-slate-900 border border-slate-800 rounded-2xl p-4 space-y-2">
                <span class="text-[10px] font-semibold text-slate-400 uppercase tracking-wider block">MCP Manifest (preview)</span>
                <pre class="bg-slate-950 p-4 rounded-xl text-xs font-mono text-indigo-300 overflow-x-auto"><code>{
  "name": "htmlbox_server",
  "version": "1.0.0",
  "capabilities": {
    "tools": ["query_kpi_metrics", "generate_spot_check"],
    "resources": ["r2://cartera_vencida.csv"]
  },
  "authentication": "Bearer hbx_live_839201948201"
}</code></pre>
              </div>
            </div>
          </div>
        </div>
      </main>
