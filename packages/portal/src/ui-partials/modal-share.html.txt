      <div x-show="shareModalOpen" x-transition.opacity class="fixed inset-0 bg-slate-950/85 backdrop-blur-md z-50 flex items-center justify-center p-4" x-cloak>
        <div class="bg-slate-900 border border-slate-800 rounded-2xl w-full max-w-2xl overflow-hidden shadow-2xl space-y-4 p-6">
          <div class="flex items-center justify-between border-b border-slate-800 pb-3">
            <div class="flex items-center gap-2">
              <div class="w-3 h-3 rounded-full bg-emerald-500 animate-pulse"></div>
              <h3 class="font-bold text-base text-slate-100">Box publicado</h3>
            </div>
            <button @click="closeShareModal()" class="text-slate-500 hover:text-white"><i class="fa-solid fa-xmark text-lg"></i></button>
          </div>
          <p class="text-xs text-slate-300">
            Sin servidores ni despliegues técnicos. Tu HTML Box está activo y accesible a través de la infraestructura aislada de HTMLBox.
          </p>

          <div class="bg-slate-950 p-3 rounded-xl border border-slate-800 flex items-center justify-between gap-3 font-mono text-xs">
            <span class="text-indigo-400 truncate" x-text="shareUrl || '—'"></span>
            <button @click="copyShareLink()" class="px-3 py-1.5 bg-indigo-600 hover:bg-indigo-500 text-white rounded-lg transition font-sans flex items-center gap-1.5 flex-shrink-0">
              <i class="fa-solid fa-copy"></i><span>Copiar Enlace</span>
            </button>
          </div>

          <div class="grid grid-cols-3 gap-3 pt-2 text-center text-xs">
            <div class="p-3 bg-slate-950/50 rounded-xl border border-slate-800">
              <i class="fa-solid fa-shield-halved text-emerald-400 text-base mb-1"></i>
              <p class="font-semibold text-slate-200">Aislamiento Sandbox</p>
              <p class="text-[10px] text-slate-400">Ejecución segura de scripts</p>
            </div>
            <div class="p-3 bg-slate-950/50 rounded-xl border border-slate-800">
              <i class="fa-solid fa-bolt text-amber-400 text-base mb-1"></i>
              <p class="font-semibold text-slate-200">Versión actual</p>
              <p class="text-[10px] text-slate-400" x-text="`v${currentBox?.htmlbox_version ?? 0}`"></p>
            </div>
            <div class="p-3 bg-slate-950/50 rounded-xl border border-slate-800">
              <i class="fa-solid fa-database text-indigo-400 text-base mb-1"></i>
              <p class="font-semibold text-slate-200">DB del box</p>
              <p class="text-[10px] text-slate-400" x-text="currentBox?.turso_status || 'pending'"></p>
            </div>
          </div>

          <div class="flex justify-between items-center pt-2">
            <span class="text-[10px] text-slate-500" x-text="`Última actualización: ${currentBox?.updated_at || '—'}`"></span>
            <button @click="closeShareModal()" class="px-4 py-2 bg-slate-800 hover:bg-slate-700 text-slate-300 rounded-xl text-xs font-medium transition">Cerrar</button>
          </div>
        </div>
      </div>

