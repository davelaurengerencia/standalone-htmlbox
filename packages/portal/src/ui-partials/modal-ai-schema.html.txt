      <div x-show="aiModalOpen" x-transition.opacity class="fixed inset-0 bg-slate-950/85 backdrop-blur-md z-50 flex items-center justify-center p-4" x-cloak @keydown.escape.window="closeAiModal()">
        <div class="bg-slate-900 border border-slate-800 rounded-2xl w-full max-w-5xl max-h-[90vh] overflow-hidden shadow-2xl flex flex-col">
          <div class="flex items-center justify-between border-b border-slate-800 px-6 py-4 flex-shrink-0">
            <div class="flex items-center gap-3">
              <div class="w-9 h-9 rounded-xl bg-gradient-to-br from-purple-500 via-indigo-500 to-pink-500 flex items-center justify-center shadow-lg shadow-purple-600/20">
                <i class="fa-solid fa-wand-magic-sparkles text-white text-sm"></i>
              </div>
              <div>
                <h3 class="font-bold text-base text-slate-100">Propuesta de la IA</h3>
                <p class="text-xs text-slate-400">Revisá y editá las tablas sugeridas antes de aplicarlas. La IA se equivoca; esta es tu última oportunidad de corregir.</p>
              </div>
            </div>
            <button @click="closeAiModal()" class="text-slate-500 hover:text-white"><i class="fa-solid fa-xmark text-lg"></i></button>
          </div>

          <div class="flex-1 overflow-y-auto p-6 space-y-6">
            <template x-for="(t, ti) in aiModalEdited" :key="ti">
              <div class="rounded-2xl bg-slate-950/60 border border-slate-800 p-4 space-y-4">
                <div class="flex items-center justify-between">
                  <span class="text-[10px] uppercase tracking-wider font-bold text-purple-400">Tabla <span x-text="ti + 1"></span></span>
                  <button @click="aiModalEdited.splice(ti, 1)" class="text-xs text-rose-400 hover:text-rose-300 flex items-center gap-1">
                    <i class="fa-solid fa-trash-can"></i> Quitar tabla
                  </button>
                </div>

                <div class="grid grid-cols-1 md:grid-cols-2 gap-3">
                  <div>
                    <label class="text-[10px] uppercase tracking-wider font-bold text-slate-400 block mb-1">Slug</label>
                    <input type="text" x-model="t.slug"
                           :class="!isValidSlug(t.slug) ? 'border-rose-500/60' : 'border-slate-800'"
                           class="w-full bg-slate-950 border rounded-lg px-3 py-1.5 text-sm font-mono focus:outline-none focus:border-indigo-500">
                    <p x-show="!isValidSlug(t.slug)" class="text-[10px] text-rose-400 mt-1">Slug inválido. minúsculas, _ permitidos, empieza con letra.</p>
                  </div>
                  <div>
                    <label class="text-[10px] uppercase tracking-wider font-bold text-slate-400 block mb-1">Nombre</label>
                    <input type="text" x-model="t.name"
                           class="w-full bg-slate-950 border border-slate-800 rounded-lg px-3 py-1.5 text-sm focus:outline-none focus:border-indigo-500">
                  </div>
                </div>

                <div>
                  <label class="text-[10px] uppercase tracking-wider font-bold text-slate-400 block mb-1">Descripción</label>
                  <textarea x-model="t.description" rows="2"
                            class="w-full bg-slate-950 border border-slate-800 rounded-lg px-3 py-1.5 text-xs focus:outline-none focus:border-indigo-500"></textarea>
                </div>

                <div>
                  <div class="flex items-center justify-between mb-2">
                    <label class="text-[10px] uppercase tracking-wider font-bold text-slate-400">Columnas</label>
                    <span class="text-[10px] text-slate-500" x-text="(t.columns || []).length + ' columna(s)'"></span>
                  </div>
                  <div class="space-y-1.5">
                    <template x-for="(c, ci) in t.columns" :key="ci">
                      <div class="flex items-center gap-2">
                        <input type="text" x-model="c.name" placeholder="nombre_columna"
                               class="flex-1 bg-slate-950 border border-slate-800 rounded-lg px-2 py-1 text-xs font-mono focus:outline-none focus:border-indigo-500">
                        <select x-model="c.type" class="bg-slate-950 border border-slate-800 rounded-lg px-2 py-1 text-xs">
                          <option value="string">string</option>
                          <option value="number">number</option>
                          <option value="boolean">boolean</option>
                          <option value="date">date</option>
                        </select>
                        <button @click="t.columns.splice(ci, 1)" class="text-rose-400 hover:text-rose-300 text-xs px-2 py-1" title="Quitar columna">
                          <i class="fa-solid fa-times"></i>
                        </button>
                      </div>
                    </template>
                    <button @click="t.columns.push({ name: '', type: 'string' })"
                            class="text-xs text-indigo-400 hover:text-indigo-300 mt-1 flex items-center gap-1">
                      <i class="fa-solid fa-plus"></i> Agregar columna
                    </button>
                  </div>
                </div>

                <div>
                  <div class="flex items-center justify-between mb-2">
                    <label class="text-[10px] uppercase tracking-wider font-bold text-slate-400">Filas de muestra</label>
                    <span class="text-[10px] text-slate-500" x-text="(t.sample_rows || []).length + ' fila(s)'"></span>
                  </div>
                  <template x-if="t.sample_rows && t.sample_rows.length > 0 && t.columns && t.columns.length > 0">
                    <div class="overflow-x-auto rounded-lg border border-slate-800">
                      <table class="w-full text-[11px]">
                        <thead class="bg-slate-900">
                          <tr>
                            <template x-for="c in t.columns" :key="c.name">
                              <th class="p-1.5 text-left font-medium text-slate-400 whitespace-nowrap" x-text="c.name || '—'"></th>
                            </template>
                            <th class="p-1.5 w-8"></th>
                          </tr>
                        </thead>
                        <tbody class="divide-y divide-slate-800/60">
                          <template x-for="(row, ri) in t.sample_rows" :key="ri">
                            <tr>
                              <template x-for="c in t.columns" :key="c.name">
                                <td class="p-1">
                                  <input type="text"
                                         x-model="t.sample_rows[ri][c.name]"
                                         class="w-full bg-slate-950 border border-slate-800/50 rounded px-1.5 py-0.5 text-[11px] focus:outline-none focus:border-indigo-500">
                                </td>
                              </template>
                              <td class="p-1">
                                <button @click="t.sample_rows.splice(ri, 1)" class="text-rose-400 hover:text-rose-300 px-1" title="Quitar fila">
                                  <i class="fa-solid fa-trash-can text-[10px]"></i>
                                </button>
                              </td>
                            </tr>
                          </template>
                        </tbody>
                      </table>
                    </div>
                  </template>
                  <template x-if="!t.sample_rows || t.sample_rows.length === 0">
                    <p class="text-[11px] text-slate-500 italic">Sin filas de muestra.</p>
                  </template>
                  <button @click="addSampleRow(t)" class="text-xs text-indigo-400 hover:text-indigo-300 mt-2 flex items-center gap-1">
                    <i class="fa-solid fa-plus"></i> Agregar fila
                  </button>
                </div>

                <div>
                  <label class="text-[10px] uppercase tracking-wider font-bold text-slate-400 block mb-1">Ejemplo SDK</label>
                  <div class="flex items-center gap-2 bg-slate-950 border border-slate-800 rounded-lg px-3 py-1.5">
                    <code class="flex-1 text-[11px] font-mono text-indigo-300 truncate" x-text="t.sdk_example"></code>
                    <button @click="copyToClipboard(t.sdk_example)" class="text-slate-400 hover:text-white text-xs flex items-center gap-1 px-2 py-0.5 rounded hover:bg-slate-800 transition">
                      <i class="fa-solid fa-copy"></i> Copiar
                    </button>
                  </div>
                </div>
              </div>
            </template>

            <template x-if="!aiModalEdited || aiModalEdited.length === 0">
              <div class="text-center py-10 text-slate-500 text-sm">
                <i class="fa-solid fa-circle-info text-2xl mb-2 block text-slate-600"></i>
                No hay tablas para aplicar. Agregá al menos una o cancelá.
              </div>
            </template>
          </div>

          <div class="flex items-center justify-between border-t border-slate-800 px-6 py-4 flex-shrink-0 bg-slate-900 gap-3 flex-wrap">
            <div class="text-xs text-slate-400 flex items-center gap-3 flex-wrap">
              <span>
                <span class="text-slate-100 font-semibold" x-text="aiModalEdited?.length || 0"></span> tabla(s) lista(s)
              </span>
              <span x-show="aiSchema?.model">
                modelo: <code class="text-indigo-300" x-text="aiSchema?.model"></code>
              </span>
              <span x-show="aiSchema?.tokensUsed != null" class="text-slate-500">
                <i class="fa-solid fa-coins text-amber-400"></i> <span x-text="aiSchema?.tokensUsed"></span> tokens
              </span>
            </div>
            <div class="flex gap-2">
              <button @click="closeAiModal()" class="px-4 py-2 bg-slate-800 hover:bg-slate-700 text-slate-300 rounded-xl text-xs font-medium transition flex items-center gap-1.5">
                <i class="fa-solid fa-xmark"></i> Cancelar
              </button>
              <button @click="aiApply()" :disabled="aiApplying || !validEditedTables()"
                      class="px-4 py-2 bg-emerald-600 hover:bg-emerald-500 disabled:bg-slate-700 disabled:cursor-not-allowed text-white rounded-xl text-xs font-medium transition flex items-center gap-1.5 shadow-lg shadow-emerald-600/20">
                <template x-if="aiApplying"><i class="fa-solid fa-spinner animate-spin"></i></template>
                <template x-if="!aiApplying"><i class="fa-solid fa-check"></i></template>
                <span x-text="aiApplying ? 'Aplicando…' : 'Aplicar ' + (aiModalEdited?.length || 0) + ' tabla(s)'"></span>
              </button>
            </div>
          </div>
        </div>
      </div>
