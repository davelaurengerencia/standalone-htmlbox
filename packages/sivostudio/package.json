{
  "name": "@htmlbox/sivostudio",
  "version": "0.1.0",
  "private": true,
  "description": "SivoStudio — experimento aislado: cada box se aprovisiona como Worker WFP desde la creación y se edita a sí mismo bajo /editor/*. Paquete separado del resto del monorepo (no comparte DB ni bindings con control-plane).",
  "scripts": {
    "dev": "wrangler dev --remote --port 8786",
    "dev:local": "wrangler dev --port 8786",
    "deploy": "wrangler deploy",
    "build": "node scripts/inline-app-studio.mjs && node scripts/build.mjs",
    "build:editor": "node scripts/inline-app-studio.mjs",
    "test": "node --test __tests__/*.test.js"
  }
}
